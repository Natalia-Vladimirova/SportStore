@model IEnumerable<Store.Models.Tovar>
@{
    ViewBag.Title = "Show compare";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@helper Truncate(string input, int length)
{
    if (input.Length <= length) {
        @input
    } else {
        @input.Substring(0, length)<text>...</text>
    }
}


<h2>Compare</h2>
@if (Model.Any()){
<div class="buttoncomparedel">@Html.ActionLink("Delete all", "DeleteAllCompare", "Store")</div>
<br />

    <table class="compareTab">
        <tr>
            <th>Image</th>
            <th>Name</th>
            <th>Price</th>
            <th>Count</th>
            <th>Description</th>
            <th></th>
        </tr>
    @foreach(var item in Model){
            <tr id="@item.TovarId">
                <td><img src="@Url.Content(item.Image)"/></td>
                <td>@Truncate(item.Title, 25)</td>
                <td>@item.Price</td>
                <td>@item.Amount</td>
                <td>@Truncate(item.Description, 50)</td>
                <td>
                    @Ajax.ActionLink("Delete", "DeleteCompare", "Store", new { id = item.TovarId }, new AjaxOptions { UpdateTargetId = item.TovarId.ToString(), InsertionMode = InsertionMode.Replace}, null)
                </td>
            </tr>
    }
    </table>
}
else {
    <p>Select products!</p>
    <br />
    <div class="buttoncomparedel"><a href="/Store/Index/">Go to the main page</a></div>
}
